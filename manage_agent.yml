- name: ========== MANAGE AGENT ==========
  hosts:
  - tc_agent
  
  vars:
    param: "{{ oper }}" 
  
  tasks:
  - name: ==================== debug =================
    shell: echo '{{param}}'
  
  - name: ==================== TC agent start =================
    service:
      name: 'teamcity-agent'
      pattern: /etc/init.d
      state: '{{param}}'
    become: yes
