- name: ===================== CHECK IF DIRECTORY '{{Tools_path}}' EXISTS =====================
  file:
    path: '{{Tools_path}}'
    state: directory
    mode: 0755
  become: yes

- name: ===================== DOWNLOAD TOOLS TO /tmp =====================
  get_url:
    url: "{{item.path}}"
    dest: "/tmp/{{item.name}}"
    url_username: __myUser__
    url_password: __myPassword__
    mode: 0440
  with_items:  "{{ ToolsList }}"

#- name: ===================== UNARCHIVE BUILD TOOLS FROM /tmp to '{{Tools_path}}'  =====================
#  unarchive:
#    src: "/tmp/{{item.name}}"
#    dest: '{{Tools_path}}'
#    remote_src: yes
#  with_items:  "{{ ToolsList }}"
#  become: yes

- name: ===================== XXX =====================
  shell: echo XXXXXX > {{Tools_path}}/XXXX
  #with_items:  "{{ ToolsList }}"
  become: yes

- name: ===================== Check files  =====================
  shell: echo "SW  {{ item.name }}"
  with_items:  "{{ ToolsList }}"


#- name: ===================== DOWNLOAD AND UNARCHIVE TOOLS =====================
#  unarchive:
#    src: "{{item.path}}"
#    dest: '{{Tools_path}}'
#    remote_src: yes
#    keep_newer: yes
#  with_items:  "{{ ToolsList }}"
#  become: yes
