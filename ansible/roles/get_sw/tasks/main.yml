- name: ===================== FIND FOLDERS =====================
  find:
    paths: '{{Tools_path}}'
    patterns: '*'
    file_type: directory
  register: file

- name: ===================== SHOW FOLDERS =====================
  #debug: msg="{{ file.file | sort(attribute='ctime') |  list }}"
  debug: msg="{{ file}}"